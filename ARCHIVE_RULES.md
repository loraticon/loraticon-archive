# 로라티콘 아카이브 구성·운영 규칙

마지막 갱신: 2026-09-18

이 문서는 `archive.loraticon.com`에 새 콘텐츠를 추가하거나 기존 콘텐츠를 수정할 때 지켜야 하는 기준입니다. 검색 노출만을 위한 빈 페이지나 링크 모음이 아니라, 방문자가 내용을 이해하고 원하는 원문·다운로드 페이지로 이동할 수 있는 독립적인 안내 페이지를 만드는 것을 원칙으로 합니다.

## 1. 페이지 유형

### A. 상품·테마 상세 페이지

- 저장 위치: 저장소 루트
- 파일명: `detail_영문_이름.html`
- 예시: `detail_cherryday_diary.html`
- 용도: 노션 템플릿 또는 꾸미기 세트의 구성, 이미지, 사용 방법, FAQ와 구매·다운로드 경로를 안내합니다.
- 메인 목록인 `index.html`에서 상세 페이지로 직접 연결합니다.

### B. 네이버 블로그 글 소개 페이지

- 저장 위치: `blog/`
- 파일명: 네이버 게시물 번호와 같은 `게시물번호.html`
- 대표 이미지: `images/blog/게시물번호.webp`
- 용도: 네이버 글의 주제와 핵심 내용을 독립적인 문장으로 소개하고 네이버 원문으로 직접 연결합니다.
- `blog/index.html`에서 각 소개 페이지로 직접 연결합니다.

## 2. 파일과 폴더 이름

- 상세 HTML 파일은 현재 규칙에 맞춰 소문자와 밑줄을 사용합니다.
- 이미지 폴더는 소문자와 하이픈을 사용합니다.
- 템플릿 이미지는 `images/template/영문-이름/`에 둡니다.
- 꾸미기 세트 이미지는 `images/deco/영문-이름/`에 둡니다.
- 프로필·파비콘은 `images/profile/`에 둡니다.
- 빈 HTML, 임시 복사본, 사용하지 않는 테스트 파일은 배포하지 않습니다.
- 이미 배포된 URL의 파일명은 특별한 이유 없이 변경하지 않습니다. 변경해야 한다면 기존 URL의 처리 방법을 먼저 정합니다.

## 3. 상세 페이지 필수 구성

상세 페이지에는 아래 항목을 가능한 한 이 순서로 둡니다.

1. 뒤로가기 또는 메인 페이지 이동 링크
2. 한 개의 명확한 `h1`
3. 대표 이미지와 한두 문장의 소개
4. 구성품 또는 핵심 기능
5. 실제 사용 화면이나 전체 구성 이미지
6. 사용 방법 또는 활용 예시
7. 실제 페이지에 보이는 FAQ
8. 관련 콘텐츠 내부 링크
9. 구매 또는 무료 다운로드 CTA

페이지 제목, 설명, 이미지, 가격과 구성 수량은 서로 다르게 적히지 않도록 한 번에 확인합니다.

## 4. 링크와 버튼

- 다른 페이지로 이동하는 요소는 반드시 실제 `<a href="…">` 링크로 만듭니다.
- 링크를 버튼처럼 보이게 만들 때도 `<button>`이나 자바스크립트 클릭 이벤트 대신 `<a>`에 스타일을 적용합니다.
- 외부 페이지를 새 창에서 열면 `target="_blank" rel="noopener noreferrer"`를 함께 사용합니다.
- CTA는 네이버 원문, 구매 페이지 또는 다운로드 페이지의 최종 URL로 직접 연결합니다.
- `자세히 보기`만 쓰기보다 `네이버 원문 읽기`, `무료로 받기`, `구매하러 가기`처럼 목적이 드러나는 문구를 사용합니다.
- 관련 콘텐츠는 실제 연관성이 있는 페이지만 연결합니다. 같은 링크를 과도하게 반복하지 않습니다.

권장 구조는 다음과 같습니다.

```text
메인 목록 → 상세 페이지 → 네이버 원문·다운로드·구매 페이지
블로그 목록 → 글 소개 페이지 → 네이버 원문
상세 페이지 ↔ 관련 상세 페이지
```

## 5. 이미지 규칙

### 파일

- 정지 이미지는 특별한 이유가 없으면 WebP를 사용합니다.
- 움직임이 꼭 필요한 경우에만 GIF 또는 애니메이션 WebP를 사용합니다.
- 실제 표시 크기보다 지나치게 큰 원본은 먼저 픽셀 크기를 줄이는 방법을 검토합니다.
- 정지 이미지는 300KB 이하를 권장하고 500KB 이상이면 최적화 가능성을 다시 확인합니다.
- 애니메이션은 2MB 이하를 권장하지만, 화질 손상이 뚜렷하면 무리하게 압축하지 않습니다. 이 용량은 배포 차단 기준이 아니라 점검 기준입니다.
- 파일명은 이미지의 역할을 알 수 있게 작성하며 `image1`, `final2` 같은 모호한 이름은 피합니다.

### HTML 속성

모든 `<img>`에는 다음 항목이 있어야 합니다.

```html
<img
  src="./images/deco/example/preview.webp"
  alt="체리와 깅엄 체크를 적용한 노션 다이어리 화면"
  width="1200"
  height="900"
  loading="lazy"
  decoding="async">
```

- `width`와 `height`에는 원본 이미지의 실제 픽셀 크기를 넣습니다.
- 페이지에서 처음 보이는 대표 이미지 한 개만 `loading="eager" fetchpriority="high"`를 사용합니다.
- 나머지 이미지는 `loading="lazy" decoding="async"`를 사용합니다.
- `alt`에는 이미지가 전달하는 내용을 짧고 구체적으로 적습니다. 장식만을 위한 이미지는 `alt=""`로 둡니다.
- 이미지가 없을 때 `onerror` 대체 이미지를 사용하더라도 원본 파일 누락 여부를 먼저 확인합니다.

## 6. 기본 SEO 메타데이터

모든 색인 대상 페이지에는 다음 항목이 필요합니다.

- `<html lang="ko">`
- 페이지마다 고유한 `<title>`
- 페이지 내용을 요약한 고유한 `meta description`
- `meta author`
- `meta robots="index, follow"`
- 자기 자신을 가리키는 절대주소 canonical
- Open Graph의 `site_name`, `type`, `locale`, `title`, `description`, `url`, `image`
- Twitter 카드의 `card`, `title`, `description`, `image`

제목과 설명에는 실제 검색 의도를 자연스럽게 반영하되 같은 단어를 부자연스럽게 반복하지 않습니다. 페이지마다 제목과 설명이 서로 달라야 합니다.

## 7. 구조화 데이터

구조화 데이터는 `<script type="application/ld+json">`으로 작성하고 화면에 보이는 내용과 일치시킵니다.

### 상품·테마 상세 페이지

- 기본 유형은 `Product`입니다.
- 필수 정보: `name`, `description`, `image`, `url`, `brand`, `offers`
- 유료 상품은 실제 `price`, `priceCurrency: KRW`, 판매 URL을 입력합니다.
- 무료 상품은 가격을 `0`으로 표시하고 실제 무료 배포 URL을 연결합니다.
- 판매·배포 중이면 `availability: https://schema.org/InStock`를 사용합니다.
- 구성 수량이 확정되어 있으면 `additionalProperty`로 기록합니다.
- 화면에 FAQ가 있을 때만 `FAQPage`를 추가합니다.
- 화면에 없는 후기, 별점, 할인 가격이나 품절 상태를 임의로 넣지 않습니다.

### 네이버 글 소개 페이지

- 유형은 `Article`을 사용합니다.
- `headline`, `description`, `url`, `mainEntityOfPage`, `inLanguage`, `author`를 입력합니다.
- 원문 네이버 URL은 `isBasedOn`에 기록합니다.
- 아카이브 페이지의 canonical은 아카이브 자기 주소로 유지합니다.

## 8. 새 콘텐츠를 추가할 때 함께 수정할 파일

### 새 상세 페이지

1. `detail_이름.html` 생성
2. `images/template/이름/` 또는 `images/deco/이름/`에 이미지 추가
3. `index.html`의 `items` 배열에 카드 추가
4. 관련 상세 페이지에 자연스러운 내부 링크 추가
5. `sitemap-v2.xml`에 상세 페이지 URL 추가

### 새 네이버 글 소개 페이지

1. `blog/게시물번호.html` 생성
2. `images/blog/게시물번호.webp` 추가
3. `blog/index.html`에 소개 카드 추가
4. 관련 상세 페이지가 있으면 양쪽에서 연결
5. `sitemap-v2.xml`에 소개 페이지 URL 추가

## 9. 사이트맵과 검색 등록

- 색인되어야 하는 정식 HTML 페이지만 `sitemap-v2.xml`에 넣습니다.
- 이미지 파일, 검색 결과 URL, 빈 페이지, 임시 페이지는 넣지 않습니다.
- 새 페이지는 새 `<url>` 항목을 추가합니다.
- 실제로 내용을 수정한 페이지의 `lastmod`만 `YYYY-MM-DD` 형식으로 갱신합니다.
- 내용이 바뀌지 않은 페이지의 날짜를 반복해서 갱신하지 않습니다.
- 기존 사이트맵 URL이 같고 Search Console에서 성공 상태라면 매번 다시 제출할 필요는 없습니다.
- 중요한 새 페이지는 배포 확인 후 Search Console URL 검사에서 색인 생성을 요청할 수 있습니다.
- `robots.txt`의 사이트맵 주소와 실제 사이트맵 주소를 항상 일치시킵니다.

## 10. 배포 전 체크리스트

### 콘텐츠

- [ ] `h1`은 한 개이며 제목이 내용을 정확하게 설명한다.
- [ ] 가격, 무료 여부, 구성 수량과 CTA가 서로 일치한다.
- [ ] 본문은 다른 페이지를 복사한 채 이름만 바꾼 내용이 아니다.
- [ ] 네이버 원문 또는 판매·다운로드 페이지로 직접 이동할 수 있다.

### 이미지와 링크

- [ ] 모든 로컬 이미지 파일이 실제로 존재한다.
- [ ] 모든 이미지에 `alt`, `width`, `height`, `loading`, `decoding`이 있다.
- [ ] 대표 이미지만 eager/high이고 나머지는 lazy이다.
- [ ] 이동 기능은 `<a href>`로 구현되어 있다.
- [ ] 외부 새 창 링크에 `rel="noopener noreferrer"`가 있다.
- [ ] 깨진 내부 링크와 불필요한 중간 경유 링크가 없다.

### SEO와 기술 검증

- [ ] title, description, canonical, OG, Twitter 메타데이터가 있다.
- [ ] canonical과 OG URL이 실제 배포 URL과 일치한다.
- [ ] JSON-LD 문법이 유효하고 화면 내용과 일치한다.
- [ ] `sitemap-v2.xml`에 새 URL 또는 수정일이 반영되어 있다.
- [ ] 빈 HTML과 임시 파일이 없다.
- [ ] 모바일 화면에서 이미지, 슬라이더, 고정 CTA가 본문을 가리지 않는다.

### 배포

- [ ] 변경 파일을 다시 확인한 뒤 커밋한다.
- [ ] GitHub Pages 배포 후 실제 URL을 열어 본다.
- [ ] 대표 이미지와 CTA 링크를 실제 사이트에서 한 번씩 확인한다.
- [ ] 중요한 신규 URL만 Search Console에서 색인 생성을 요청한다.

## 11. 보호해야 하는 파일

- Google 소유권 확인 파일인 `googled674a6255dd12586.html`은 확인 없이 삭제하거나 이름을 바꾸지 않습니다.
- `robots.txt`, canonical, 사이트 도메인과 사이트맵 파일명은 사이트 전체에 영향을 주므로 변경 전 연결 관계를 확인합니다.
- 기존 공개 페이지 URL은 검색 결과와 외부 링크가 연결되어 있을 수 있으므로 단순 정리 목적으로 삭제하지 않습니다.
